# Frontend env. Copy to .env.local for local dev (optional).
# URL of the deployed BEACON advisor backend (Render). If unset, the app uses
# http://localhost:8787 in dev and simply falls back to the offline AI Advisor
# if no backend is reachable.
VITE_API_URL=https://beacon-advisor.onrender.com
